import UpdateCompanyInfoView from './UpdateCompanyInfoView'

describe('@views/UpdateCompanyInfoView', () => {
  xit('is a valid view', () => {
    expect(UpdateCompanyInfoView).toBeAViewComponent()
  })
})
